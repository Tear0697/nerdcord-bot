/// <reference types="node" />
import { EventEmitter } from "events";
export default class Client extends EventEmitter {
    private socket;
    login(token: string): Promise<void>;
    createMessage(content: string, channelID: string, token: string): Promise<void>;
    createEmbed(description: string, channelID: string, token: string, title: string): Promise<void>;
}
